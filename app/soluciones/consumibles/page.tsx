import type {Metadata} from 'next';import {ProductCatalog} from '@/components/PrototypePages';export const metadata:Metadata={title:'Consumibles y refacciones | SOLINSA',description:'Columnas, viales, septas, lámparas y refacciones.'};export default function Page(){return <ProductCatalog/>}
